# Quick test: 30° only
input: "data/151675.h5ad"
seed: 2026
angles: [30]
data_type: "sequencing"

simulation:
  parameter_mode: "hungarian"
  spatial_mode: "reference_rank"
  min_cells: 30
  verbose: true

alterations:
  - id: baseline
    type: baseline
    fold_change: 1.0

transformation:
  center_correction: 0
  keep_bounds: true
